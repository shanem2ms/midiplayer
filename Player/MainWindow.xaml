<Window x:Class="midiplayer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:midiplayer"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="50" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="50" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="50"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="50"/>
        </Grid.ColumnDefinitions>
        <Label Grid.Row="1" Grid.Column="1" Content="{Binding Path=Value, ElementName=VolumeSlider}" />
        <Slider x:Name="VolumeSlider" Grid.Row="2" Grid.Column="1" Orientation="Vertical" Value="100" Minimum="0" Maximum="100" 
                ValueChanged="VolumeSlider_ValueChanged"/>
        <ListBox Grid.Row="2" Grid.Column="2" ItemsSource="{Binding Path=MidiFiles}" />
        <Label Grid.Row="3" Grid.Column="1" Content="Volume"/>
    </Grid>
</Window>
