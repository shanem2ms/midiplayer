<UserControl x:Class="PlayerWPF.Sequencer"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PlayerWPF"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <ScrollViewer HorizontalScrollBarVisibility="Visible" VerticalScrollBarVisibility="Hidden">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Canvas x:Name="TimeStep" Height="25" Background="AliceBlue" HorizontalAlignment="Left">
            </Canvas>
            <Canvas Height="25">
                <Line x:Name="currentPosLine" Stroke="Blue" StrokeThickness="5"  
                  Y2="{Binding RelativeSource={RelativeSource FindAncestor, 
                AncestorType={x:Type Canvas}},Path=ActualHeight}"
                      Panel.ZIndex="2"/>
            </Canvas>
            <StackPanel Orientation="Vertical" x:Name="Channels" Grid.Row="1"/>
        </Grid>
    </ScrollViewer>
</UserControl>
